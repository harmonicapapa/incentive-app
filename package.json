{
  "name": "earned-ledger",
  "version": "1.0.0",
  "description": "Earned: a monthly task and incentive ledger with a parent portal and a read-only progress view",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node tests/calc.test.js && node --test tests/server.test.mjs"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@netlify/blobs": "^11.1.0"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  }
}
